import type { ProblemDefinition } from '@/lib/types'

/**
 * Accepted community submissions live here (merged into PROBLEMS at build time).
 * See CONTRIBUTING.md for the review process.
 */
export const COMMUNITY_PROBLEMS: ProblemDefinition[] = []
