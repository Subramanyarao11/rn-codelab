# Environment variables (optional)

# Public repo slug for contribute links and issue fallback URLs
NEXT_PUBLIC_GITHUB_REPO=Subramanyarao11/rn-codelab

# Optional: enable one-click form submission → GitHub Issue (server route)
# Create a fine-grained PAT with "Issues: Read and write" on the repo
# GITHUB_REPO=Subramanyarao11/rn-codelab
# GITHUB_TOKEN=
